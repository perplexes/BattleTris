\* reset_ack OFF: a reconnect resets the client seq but not the ack -> AckBounds violated (snap-back).
CONSTANTS
    MaxSeq = 4
    MaxChan = 2
    AckOnBarrierReject = TRUE
    ResetAckOnReattach = FALSE
    LeaveNeedsAuth = TRUE
    DefensiveReLeave = TRUE
INIT Init
NEXT Next
INVARIANT AckBounds
