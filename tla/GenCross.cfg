\* Generator: emit the shortest trace that performs a bazaar CROSSING (a gameplay or
\* weapon input the barrier rejects). All knobs at their SHIPPED values.
CONSTANTS
    MaxSeq = 4
    MaxChan = 3
    AckOnBarrierReject = TRUE
    ResetAckOnReattach = TRUE
INIT Init
NEXT Next
INVARIANT NotCrossed
