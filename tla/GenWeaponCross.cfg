\* Generator: a weapon is APPLIED during normal play, then a later input CROSSES the
\* barrier — exercises both the weapon-delivery path and the barrier-reject ack in one
\* trace. All knobs shipped.
CONSTANTS
    MaxSeq = 5
    MaxChan = 3
    AckOnBarrierReject = TRUE
    ResetAckOnReattach = TRUE
INIT Init
NEXT Next
INVARIANT NotWeaponThenCross
