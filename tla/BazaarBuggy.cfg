\* The BUG: a barrier-rejected input does NOT advance ack. NoDeadlock is violated —
\* Apalache finds the absorbing stuck state and prints a counterexample trace.
CONSTANTS
    MaxSeq = 4
    MaxChan = 3
    AckOnBarrierReject = FALSE
INIT Init
NEXT Next
INVARIANT NoDeadlock
